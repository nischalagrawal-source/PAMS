{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Push & Deploy",
      "type": "shell",
      "command": "git add -A; $msg = Read-Host 'Commit message'; if ($msg) { git commit -m $msg; git push origin master } else { Write-Host 'Aborted — no message entered.' -ForegroundColor Yellow }",
      "options": {
        "shell": {
          "executable": "powershell.exe",
          "args": ["-NoProfile", "-Command"]
        }
      },
      "presentation": {
        "reveal": "always",
        "panel": "shared",
        "focus": true
      },
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "problemMatcher": []
    }
  ]
}
